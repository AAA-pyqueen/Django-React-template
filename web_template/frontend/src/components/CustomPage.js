import React from 'react';
import CustomLayout from './CustomLayout';
import './custom_styles.css';

function CustomPage() {
    return (
        <CustomLayout>
            <h2>Custom Page</h2>
            <p>This is a custom page in the React frontend.</p>
        </CustomLayout>
    );
}

export default CustomPage;